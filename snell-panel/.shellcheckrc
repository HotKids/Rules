# snell-panel 的运维脚本刻意采用紧凑单行风格（`A && B || C`、成组的空初始化
# `VAR= VAR2=`、在受控上下文里的非引用展开等）。以下 shellcheck 码属于风格/本地化
# 提示或对该风格的误报，统一在项目级关闭，保留其余结构性检查供 CI 拦截真正的错误。
#
# SC2015 — `A && B || C` 非 if-then-else（本仓库大量用作紧凑守卫，语义已知）
# SC1007 — `VAR=` 成组空初始化后的空格
# SC2086 — 受控数值/协议名（端口、tcp/udp）的非引用展开
# SC2068 — run_cmd 里对已知参数列表的 `$@` 展开
# SC2002 — “useless cat”（管道可读性取舍）
# SC2119 — 函数按默认无参调用
# SC2155 — declare 与赋值同一行
# SC2034 — 解析但有意忽略的 --name（NAME）
# SC2018/SC2019 — `tr A-Z a-z` 未用 [:upper:]/[:lower:]（仅 ASCII，足够）
# SC1087 — grep 正则 `:$p[[:space:]]` 被误判为数组下标展开
disable=SC2015,SC1007,SC2086,SC2068,SC2002,SC2119,SC2155,SC2034,SC2018,SC2019,SC1087
