# ════════════════════════════════════════════════════════════════
# sync-config.txt — Surge Profile → Clash Sample.yaml 映射表
# ════════════════════════════════════════════════════════════════
#
# 分区格式（# >> SectionName）：
#
#   # >> URL Mapping
#     X => Y    完整 URL 精确替换、路径前缀替换、或仓库简写
#               仓库简写：owner/repo/branch（自动补全 raw.githubusercontent.com）
#
#   # >> Builtin
#     X => Y    Surge 内置规则集名称 → 真实 Clash URL
#
#   # >> Skip
#     keyword   整链路跳过：URL / 策略名 / 组名 含此关键词时全部跳过
#
#   # >> Group
#     group => <name>
#     ...       Clash 专属 proxy-group YAML 块，原样注入 proxy-groups
#     end
#               优先级高于 skip 和普通转换
# ════════════════════════════════════════════════════════════════

# >> URL Mapping

# Loyalsoldier: surge-rules → clash-rules（仓库简写，自动补全 raw URL）
Loyalsoldier/surge-rules/release => Loyalsoldier/clash-rules/release

# VirgilClyne: .list → .yaml（完整 URL 精确替换）
https://raw.githubusercontent.com/VirgilClyne/GetSomeFries/main/ruleset/HTTPDNS.Block.list => https://raw.githubusercontent.com/VirgilClyne/GetSomeFries/main/ruleset/HTTPDNS.Block.yaml
https://raw.githubusercontent.com/VirgilClyne/GetSomeFries/main/ruleset/ASN.Telegram.list => https://raw.githubusercontent.com/VirgilClyne/GetSomeFries/main/ruleset/ASN.Telegram.yaml
https://raw.githubusercontent.com/VirgilClyne/GetSomeFries/main/ruleset/ASN.China.list => https://raw.githubusercontent.com/VirgilClyne/GetSomeFries/main/ruleset/ASN.China.yaml

# >> Builtin

# Surge 内置 LAN 规则集 → Clash IP CIDR provider
LAN => https://raw.githubusercontent.com/Loyalsoldier/clash-rules/release/lancidr.txt

# >> Skip

# ConnersHua/RuleGo: 无公开 Clash 镜像路径
ConnersHua

# NSRingo: Apple News（iOS 限制，Clash 无对应策略）
NSRingo

# Speedtest: SukkaW domainset 无 Clash 兼容镜像
Speedtest

# Telegram
Telegram

# Credit / PayPal
Credit
PayPal

# >> Group

# 说明：
#   - 若 group 名在 [Proxy Group] 中存在 → 替换 Surge 转换，原样注入
#   - 若 group 名不存在于 Surge      → 追加到 proxy-groups 末尾
#   - 优先级高于 skip 和普通转换

group => 🚧 AdGuard
  - name: "🚧 AdGuard"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Clubhouse_2.png
    proxies:
      - ⛔️ REJECT
      - 🔘 DIRECT
end

group => ⛔️ REJECT
  - name: "⛔️ REJECT"
    type: select
    hidden: true
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Reject.png
    proxies:
      - REJECT
end

group => 🔘 DIRECT
  - name: "🔘 DIRECT"
    type: select
    hidden: true
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Direct.png
    proxies:
      - DIRECT
end
